[project]
name = "jpeg-dimples"
version = "0.1.0"
description = "A small implementation of Agarwal and Farid's JPEG-dimple detector"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0",
    "pillow>=10.0",
    "scipy>=1.13",
]

[tool.uv]
package = false
